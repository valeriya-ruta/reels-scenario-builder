// Proof-harness stub for the `server-only` guard. The real package throws when
// imported outside a React Server Component graph; the proof harness runs the
// real server modules directly under Node to capture their behavior, so here it
// is an empty, side-effect-free module.
export {};
